@model FoodieSocial.Models.User_post

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>CreatePost</title>
</head>
<body>
    @using (Html.BeginForm("CreatePost", "Post", FormMethod.Post, new { @enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()

            <input type="file" class="btn btn-outline-primary ms-1, hidden" id="mediaImageFile" accept="image/*" />

            <img id="display_image" src="#" class="hidden"/>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-outline-primary ms-1" />
                    <a href="@Url.Action("Index","Home")" class="btn btn-outline-primary ms-1">Back to Main page</a>
                </div>
            </div>
        }

    @*@using (Html.BeginForm("CreatePost", "Post", FormMethod.Post, new { @enctype = "multipart/form-data" }))

    {

        @Html.TextBox("mediaImageFile", "", new { type = "file", accept = "image/*" })

        <br />

        <img id="preview" src="#" width="150" height="100" class="hidden" />

        <br />

        <input type="submit" value="Create" class="btn btn-outline-primary ms-1" />

    }*@


    @*<script>
        const mediaImageFile = document.querySelector("#mediaImageFile");

            mediaImageFile.addEventListener("change", function () {
                const file = this.files[0];
                const reader = new FileReader();

                reader.addEventListener("load", () => {
                    const imageUrl = reader.result;

                    const imageElement = document.createElement("img");
                    imageElement.src = imageUrl;

                    const displayImageElement = document.querySelector("#display_image");
                    displayImageElement.innerHTML = ""; // Xóa bỏ các ảnh trước đó (nếu có)
                    displayImageElement.appendChild(imageElement);
                });

                if (file) {
                    reader.readAsDataURL(file);
                }
            });
        </script>*@

    @* script của upload *@
    <script>

        var mediaImageFile = document.getElementById('mediaImageFile');
        var display_image = document.getElementById('display_image');

        mediaImageFile.onchange = evt => {

            const [file] = mediaImageFile.files

            if (file) {

                display_image.src = URL.createObjectURL(file);

                $("#display_image").removeClass("hidden");

            }

        }
    </script>

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            flex-direction: column;
            height: 90vh;
        }

        #display_image {
            width: 100%;
            height: 200px;
            border: 1px solid black;
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 10px;
            margin-top: 10px;
            margin-bottom: 10px;
        }

            #display_image img {
                max-width: 100%;
                max-height: 100%;
            }
    </style>

</body>
</html>
